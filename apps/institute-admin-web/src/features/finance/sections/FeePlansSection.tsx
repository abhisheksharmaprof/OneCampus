import type { FinanceSectionProps } from './shared'

export default function FeePlansSection(_props: FinanceSectionProps) {
  return <div className="fin-state fin-state--empty">Coming in a later task.</div>
}
