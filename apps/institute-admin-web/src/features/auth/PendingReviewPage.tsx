import { CheckCircle2, Clock3, FileText, GraduationCap } from 'lucide-react'
import type { SessionData } from './auth.api'

export function PendingReviewPage({ session, onSignOut }: { session: SessionData; onSignOut: () => void }) {
  const declined = session.onboarding?.status === 'declined'
  return <main className="pending-page"><header className="onboarding-header"><span className="onboarding-brand"><GraduationCap /> CampusOne</span><button type="button" onClick={onSignOut}>Sign out</button></header><section className={`pending-card${declined ? ' is-declined' : ''}`}><p className="auth-eyebrow">Application status</p><h1>{declined ? 'Your application needs changes' : 'Thanks — your application is in review'}</h1>{declined ? <p>{session.onboarding?.rejectionReason || 'Please update your application and resubmit it for review.'}</p> : <><div className="review-tracker"><span className="active"><CheckCircle2 /> Submitted</span><i /><span className="active"><Clock3 /> Under review</span><i /><span>Approved</span></div><p>We’ll notify you when a platform administrator approves your institute.</p>{session.onboarding?.publicUrl && <div className="review-doc"><FileText /><div><strong>Your institute URL</strong><br /><span className="pending-badge">{session.onboarding.publicUrl}</span></div></div>}<div className="review-doc"><FileText /><div><strong>Additional details</strong><br /><span className="pending-badge">Complete after approval in Institute Details</span></div></div></>}<button className="secondary" type="button">{declined ? 'Update & resubmit' : 'Edit application'}</button><a href="mailto:support@campusone.local">Contact support</a></section></main>
}
